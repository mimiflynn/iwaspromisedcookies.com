{
  "dependencies": {
    "moment": "^2.29.4",
    "underscore": "1.4.x",
    "typogr": "0.5.x"
  },
  "private": "true",
  "scripts": {
    "clean": "rm -rf docs",
    "start": "wintersmith preview",
    "build": "npm run clean && wintersmith build && mv build docs && cp CNAME docs/CNAME"
  }
}
